# This file was generated using Kotlin DSL (.github/workflows/publish.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/krzema12/github-actions-kotlin-dsl

name: Build, Cache then Publish

on:
  push:
    branches:
      - 'main'

env:
  ASOFT_MAVEN_PGP_PRIVATE_KEY: ${{ secrets.ASOFT_MAVEN_PGP_PRIVATE_KEY }}
  ASOFT_MAVEN_PGP_PASSWORD: ${{ secrets.ASOFT_MAVEN_PGP_PASSWORD }}
  ASOFT_NEXUS_PASSWORD: ${{ secrets.ASOFT_NEXUS_PASSWORD }}
  ASOFT_NEXUS_USERNAME: ${{ secrets.ASOFT_NEXUS_USERNAME }}

jobs:
  "functions-builder":
    runs-on: "ubuntu-latest"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: functions
        run: chmod +x ./gradlew
      - id: step-3
        name: ./gradlew :functions-core:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :functions-core:build
          build-root-directory: ./functions
  "expect-builder":
    runs-on: "ubuntu-latest"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: expect
        run: chmod +x ./gradlew
      - id: step-3
        name: ./gradlew :expect-core:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :expect-core:build
          build-root-directory: ./expect
      - id: step-4
        name: ./gradlew :expect-coroutines:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :expect-coroutines:build
          build-root-directory: ./expect
  "koncurrent-primitives-builder":
    runs-on: "ubuntu-latest"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: koncurrent
        run: chmod +x ./gradlew
      - id: step-3
        name: ./gradlew :koncurrent-primitives-core:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :koncurrent-primitives-core:build
          build-root-directory: ./koncurrent
      - id: step-4
        name: ./gradlew :koncurrent-primitives-coroutines:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :koncurrent-primitives-coroutines:build
          build-root-directory: ./koncurrent
      - id: step-5
        name: ./gradlew :koncurrent-primitives-mock:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :koncurrent-primitives-mock:build
          build-root-directory: ./koncurrent
  "koncurrent-later-builder":
    runs-on: "ubuntu-latest"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: koncurrent
        run: chmod +x ./gradlew
      - id: step-3
        name: ./gradlew :koncurrent-later-core:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :koncurrent-later-core:build
          build-root-directory: ./koncurrent
      - id: step-4
        name: ./gradlew :koncurrent-later-coroutines:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :koncurrent-later-coroutines:build
          build-root-directory: ./koncurrent
      - id: step-5
        name: ./gradlew :koncurrent-later-test:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :koncurrent-later-test:build
          build-root-directory: ./koncurrent
  "live-builder":
    runs-on: "ubuntu-latest"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: live
        run: chmod +x ./gradlew
      - id: step-3
        name: ./gradlew :live-core:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-core:build
          build-root-directory: ./live
      - id: step-4
        name: ./gradlew :live-compose:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-compose:build
          build-root-directory: ./live
      - id: step-5
        name: ./gradlew :live-coroutines:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-coroutines:build
          build-root-directory: ./live
      - id: step-6
        name: ./gradlew :live-react:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-react:build
          build-root-directory: ./live
      - id: step-7
        name: ./gradlew :live-test:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-test:build
          build-root-directory: ./live
  "viewmodel-builder":
    runs-on: "ubuntu-latest"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: viewmodel
        run: chmod +x ./gradlew
      - id: step-3
        name: ./gradlew :viewmodel-core:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-core:build
          build-root-directory: ./viewmodel
  "cache-builder":
    runs-on: "ubuntu-latest"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: cache
        run: chmod +x ./gradlew
      - id: step-3
        name: ./gradlew :cache-api:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :cache-api:build
          build-root-directory: ./cache
      - id: step-4
        name: ./gradlew :cache-browser:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :cache-browser:build
          build-root-directory: ./cache
      - id: step-5
        name: ./gradlew :cache-file:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :cache-file:build
          build-root-directory: ./cache
      - id: step-6
        name: ./gradlew :cache-mock:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :cache-mock:build
          build-root-directory: ./cache
      - id: step-7
        name: ./gradlew :cache-react-native:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :cache-react-native:build
          build-root-directory: ./cache
  "math-builder":
    runs-on: "ubuntu-latest"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: math
        run: chmod +x ./gradlew
      - id: step-3
        name: ./gradlew :math-core:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :math-core:build
          build-root-directory: ./math
  "math-spatial-builder":
    runs-on: "ubuntu-latest"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: math/spatial
        run: chmod +x ./gradlew
      - id: step-3
        name: ./gradlew :math-spatial-core:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :math-spatial-core:build
          build-root-directory: ./math/spatial
  "math-vector-builder":
    runs-on: "ubuntu-latest"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: math/vector
        run: chmod +x ./gradlew
      - id: step-3
        name: ./gradlew :math-vector-core:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :math-vector-core:build
          build-root-directory: ./math/vector
  "math-point-builder":
    runs-on: "ubuntu-latest"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: math/point
        run: chmod +x ./gradlew
      - id: step-3
        name: ./gradlew :math-point-core:build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :math-point-core:build
          build-root-directory: ./math/point
  "rendezvous":
    runs-on: "ubuntu-latest"
    needs:
      - "functions-builder"
      - "expect-builder"
      - "koncurrent-primitives-builder"
      - "koncurrent-later-builder"
      - "live-builder"
      - "viewmodel-builder"
      - "cache-builder"
      - "math-builder"
      - "math-spatial-builder"
      - "math-vector-builder"
      - "math-point-builder"
    steps:
      - id: step-0
        run: echo "all builds completed. Beginning deployment"
  "functions-publisher":
    runs-on: "ubuntu-latest"
    needs:
      - "rendezvous"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: functions
        run: chmod +x ./gradlew
      - id: step-3
        name: publishing functions-core
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :functions-core:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./functions
  "expect-publisher":
    runs-on: "ubuntu-latest"
    needs:
      - "rendezvous"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: expect
        run: chmod +x ./gradlew
      - id: step-3
        name: publishing expect-core
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :expect-core:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./expect
      - id: step-4
        name: publishing expect-coroutines
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :expect-coroutines:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./expect
  "koncurrent-primitives-publisher":
    runs-on: "ubuntu-latest"
    needs:
      - "rendezvous"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: koncurrent
        run: chmod +x ./gradlew
      - id: step-3
        name: publishing koncurrent-primitives-core
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :koncurrent-primitives-core:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./koncurrent
      - id: step-4
        name: publishing koncurrent-primitives-coroutines
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :koncurrent-primitives-coroutines:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./koncurrent
      - id: step-5
        name: publishing koncurrent-primitives-mock
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :koncurrent-primitives-mock:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./koncurrent
  "koncurrent-later-publisher":
    runs-on: "ubuntu-latest"
    needs:
      - "rendezvous"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: koncurrent
        run: chmod +x ./gradlew
      - id: step-3
        name: publishing koncurrent-later-core
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :koncurrent-later-core:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./koncurrent
      - id: step-4
        name: publishing koncurrent-later-coroutines
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :koncurrent-later-coroutines:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./koncurrent
      - id: step-5
        name: publishing koncurrent-later-test
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :koncurrent-later-test:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./koncurrent
  "live-publisher":
    runs-on: "ubuntu-latest"
    needs:
      - "rendezvous"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: live
        run: chmod +x ./gradlew
      - id: step-3
        name: publishing live-core
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-core:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./live
      - id: step-4
        name: publishing live-compose
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-compose:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./live
      - id: step-5
        name: publishing live-coroutines
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-coroutines:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./live
      - id: step-6
        name: publishing live-react
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-react:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./live
      - id: step-7
        name: publishing live-test
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-test:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./live
  "viewmodel-publisher":
    runs-on: "ubuntu-latest"
    needs:
      - "rendezvous"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: viewmodel
        run: chmod +x ./gradlew
      - id: step-3
        name: publishing viewmodel-core
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-core:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./viewmodel
  "cache-publisher":
    runs-on: "ubuntu-latest"
    needs:
      - "rendezvous"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: cache
        run: chmod +x ./gradlew
      - id: step-3
        name: publishing cache-api
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :cache-api:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./cache
      - id: step-4
        name: publishing cache-browser
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :cache-browser:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./cache
      - id: step-5
        name: publishing cache-file
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :cache-file:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./cache
      - id: step-6
        name: publishing cache-mock
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :cache-mock:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./cache
      - id: step-7
        name: publishing cache-react-native
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :cache-react-native:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./cache
  "math-publisher":
    runs-on: "ubuntu-latest"
    needs:
      - "rendezvous"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: math
        run: chmod +x ./gradlew
      - id: step-3
        name: publishing math-core
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :math-core:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./math
  "math-spatial-publisher":
    runs-on: "ubuntu-latest"
    needs:
      - "rendezvous"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: math/spatial
        run: chmod +x ./gradlew
      - id: step-3
        name: publishing math-spatial-core
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :math-spatial-core:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./math/spatial
  "math-vector-publisher":
    runs-on: "ubuntu-latest"
    needs:
      - "rendezvous"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: math/vector
        run: chmod +x ./gradlew
      - id: step-3
        name: publishing math-vector-core
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :math-vector-core:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./math/vector
  "math-point-publisher":
    runs-on: "ubuntu-latest"
    needs:
      - "rendezvous"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: math/point
        run: chmod +x ./gradlew
      - id: step-3
        name: publishing math-point-core
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :math-point-core:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./math/point

