# This file was generated using Kotlin DSL (.github/workflows/publish.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/krzema12/github-actions-kotlin-dsl

name: Build, Cache then Publish

on:
  push:
    branches:
      - 'main'

env:
  ASOFT_MAVEN_PGP_PRIVATE_KEY: ${{ secrets.ASOFT_MAVEN_PGP_PRIVATE_KEY }}
  ASOFT_MAVEN_PGP_PASSWORD: ${{ secrets.ASOFT_MAVEN_PGP_PASSWORD }}
  ASOFT_NEXUS_PASSWORD: ${{ secrets.ASOFT_NEXUS_PASSWORD }}
  ASOFT_NEXUS_USERNAME: ${{ secrets.ASOFT_NEXUS_USERNAME }}

jobs:
  "live-builder":
    runs-on: "macos-latest"
    env:
      INCLUDE_BUILD: false
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: live
        run: chmod +x ./gradlew
      - id: step-3
        name: building live-core [INCLUDE_BUILD=false]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-core:build
          build-root-directory: ./live
        env:
          INCLUDE_BUILD: false
      - id: step-4
        name: building live-core [INCLUDE_BUILD=true]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-core:build
          build-root-directory: ./live
        env:
          INCLUDE_BUILD: true
      - id: step-5
        name: building live-coroutines [INCLUDE_BUILD=false]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-coroutines:build
          build-root-directory: ./live
        env:
          INCLUDE_BUILD: false
      - id: step-6
        name: building live-coroutines [INCLUDE_BUILD=true]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-coroutines:build
          build-root-directory: ./live
        env:
          INCLUDE_BUILD: true
      - id: step-7
        name: building live-react [INCLUDE_BUILD=false]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-react:build
          build-root-directory: ./live
        env:
          INCLUDE_BUILD: false
      - id: step-8
        name: building live-react [INCLUDE_BUILD=true]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-react:build
          build-root-directory: ./live
        env:
          INCLUDE_BUILD: true
      - id: step-9
        name: building live-test [INCLUDE_BUILD=false]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-test:build
          build-root-directory: ./live
        env:
          INCLUDE_BUILD: false
      - id: step-10
        name: building live-test [INCLUDE_BUILD=true]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-test:build
          build-root-directory: ./live
        env:
          INCLUDE_BUILD: true
  "viewmodel-builder":
    runs-on: "macos-latest"
    env:
      INCLUDE_BUILD: false
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: viewmodel
        run: chmod +x ./gradlew
      - id: step-3
        name: building viewmodel-core [INCLUDE_BUILD=false]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-core:build
          build-root-directory: ./viewmodel
        env:
          INCLUDE_BUILD: false
      - id: step-4
        name: building viewmodel-core [INCLUDE_BUILD=true]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-core:build
          build-root-directory: ./viewmodel
        env:
          INCLUDE_BUILD: true
      - id: step-5
        name: building viewmodel-coroutines [INCLUDE_BUILD=false]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-coroutines:build
          build-root-directory: ./viewmodel
        env:
          INCLUDE_BUILD: false
      - id: step-6
        name: building viewmodel-coroutines [INCLUDE_BUILD=true]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-coroutines:build
          build-root-directory: ./viewmodel
        env:
          INCLUDE_BUILD: true
      - id: step-7
        name: building viewmodel-react [INCLUDE_BUILD=false]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-react:build
          build-root-directory: ./viewmodel
        env:
          INCLUDE_BUILD: false
      - id: step-8
        name: building viewmodel-react [INCLUDE_BUILD=true]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-react:build
          build-root-directory: ./viewmodel
        env:
          INCLUDE_BUILD: true
      - id: step-9
        name: building viewmodel-test [INCLUDE_BUILD=false]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-test:build
          build-root-directory: ./viewmodel
        env:
          INCLUDE_BUILD: false
      - id: step-10
        name: building viewmodel-test [INCLUDE_BUILD=true]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-test:build
          build-root-directory: ./viewmodel
        env:
          INCLUDE_BUILD: true
  "rendezvous":
    runs-on: "ubuntu-latest"
    needs:
      - "live-builder"
      - "viewmodel-builder"
    steps:
      - id: step-0
        run: echo "all builds completed. Beginning deployment"
  "live-publisher":
    runs-on: "macos-latest"
    needs:
      - "rendezvous"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: live
        run: chmod +x ./gradlew
      - id: step-3
        name: building live-core [INCLUDE_BUILD=false]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-core:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./live
        env:
          INCLUDE_BUILD: false
      - id: step-4
        name: publishing live-core [INCLUDE_BUILD=true]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-core:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./live
        env:
          INCLUDE_BUILD: true
      - id: step-5
        name: building live-coroutines [INCLUDE_BUILD=false]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-coroutines:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./live
        env:
          INCLUDE_BUILD: false
      - id: step-6
        name: publishing live-coroutines [INCLUDE_BUILD=true]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-coroutines:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./live
        env:
          INCLUDE_BUILD: true
      - id: step-7
        name: building live-react [INCLUDE_BUILD=false]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-react:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./live
        env:
          INCLUDE_BUILD: false
      - id: step-8
        name: publishing live-react [INCLUDE_BUILD=true]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-react:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./live
        env:
          INCLUDE_BUILD: true
      - id: step-9
        name: building live-test [INCLUDE_BUILD=false]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-test:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./live
        env:
          INCLUDE_BUILD: false
      - id: step-10
        name: publishing live-test [INCLUDE_BUILD=true]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-test:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./live
        env:
          INCLUDE_BUILD: true
  "viewmodel-publisher":
    runs-on: "macos-latest"
    needs:
      - "rendezvous"
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: corretto
      - id: step-2
        name: Make ./gradlew executable
        working-directory: viewmodel
        run: chmod +x ./gradlew
      - id: step-3
        name: building viewmodel-core [INCLUDE_BUILD=false]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-core:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./viewmodel
        env:
          INCLUDE_BUILD: false
      - id: step-4
        name: publishing viewmodel-core [INCLUDE_BUILD=true]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-core:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./viewmodel
        env:
          INCLUDE_BUILD: true
      - id: step-5
        name: building viewmodel-coroutines [INCLUDE_BUILD=false]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-coroutines:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./viewmodel
        env:
          INCLUDE_BUILD: false
      - id: step-6
        name: publishing viewmodel-coroutines [INCLUDE_BUILD=true]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-coroutines:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./viewmodel
        env:
          INCLUDE_BUILD: true
      - id: step-7
        name: building viewmodel-react [INCLUDE_BUILD=false]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-react:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./viewmodel
        env:
          INCLUDE_BUILD: false
      - id: step-8
        name: publishing viewmodel-react [INCLUDE_BUILD=true]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-react:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./viewmodel
        env:
          INCLUDE_BUILD: true
      - id: step-9
        name: building viewmodel-test [INCLUDE_BUILD=false]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-test:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./viewmodel
        env:
          INCLUDE_BUILD: false
      - id: step-10
        name: publishing viewmodel-test [INCLUDE_BUILD=true]
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-test:publishToSonatype closeAndReleaseStagingRepository
          build-root-directory: ./viewmodel
        env:
          INCLUDE_BUILD: true

