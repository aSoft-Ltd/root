# This file was generated using Kotlin DSL (.github/workflows/publish.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/krzema12/github-actions-kotlin-dsl

name: Build, Cache then Publish

on:
  push:
    branches:
      - 'main'

env:
  ASOFT_MAVEN_PGP_PRIVATE_KEY: ${{ secrets.ASOFT_MAVEN_PGP_PRIVATE_KEY }}
  ASOFT_MAVEN_PGP_PASSWORD: ${{ secrets.ASOFT_MAVEN_PGP_PASSWORD }}
  ASOFT_NEXUS_PASSWORD: ${{ secrets.ASOFT_NEXUS_PASSWORD }}
  ASOFT_NEXUS_USERNAME: ${{ secrets.ASOFT_NEXUS_USERNAME }}
  INCLUDE_BUILD: true

jobs:
  "rendezvous":
    runs-on: "ubuntu-latest"
    steps:
      - id: step-0
        run: echo "all builds completed. Beginning deployment"
  "live-publisher":
    runs-on: "macos-latest"
    needs:
      - "rendezvous"
    env:
      INCLUDE_BUILD: true
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: zulu
      - id: step-2
        name: Make ./gradlew executable
        working-directory: live
        run: chmod +x ./gradlew
      - id: step-3
        name: publishing live-core
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-core:publishToSonatype -x :live-core:compileCommonMainKotlinMetadata closeAndReleaseStagingRepository
          build-root-directory: ./live
      - id: step-4
        name: publishing live-coroutines
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-coroutines:publishToSonatype -x :live-coroutines:compileCommonMainKotlinMetadata closeAndReleaseStagingRepository
          build-root-directory: ./live
      - id: step-5
        name: publishing live-react
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-react:publishToSonatype -x :live-react:compileCommonMainKotlinMetadata closeAndReleaseStagingRepository
          build-root-directory: ./live
      - id: step-6
        name: publishing live-test
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :live-test:publishToSonatype -x :live-test:compileCommonMainKotlinMetadata closeAndReleaseStagingRepository
          build-root-directory: ./live
  "viewmodel-publisher":
    runs-on: "macos-latest"
    needs:
      - "rendezvous"
    env:
      INCLUDE_BUILD: true
    steps:
      - id: step-0
        uses: actions/checkout@v3
        with:
          submodules: true
      - id: step-1
        uses: actions/setup-java@v3
        with:
          java-version: 18
          distribution: zulu
      - id: step-2
        name: Make ./gradlew executable
        working-directory: viewmodel
        run: chmod +x ./gradlew
      - id: step-3
        name: publishing viewmodel-core
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-core:publishToSonatype -x :viewmodel-core:compileCommonMainKotlinMetadata closeAndReleaseStagingRepository
          build-root-directory: ./viewmodel
      - id: step-4
        name: publishing viewmodel-coroutines
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-coroutines:publishToSonatype -x :viewmodel-coroutines:compileCommonMainKotlinMetadata closeAndReleaseStagingRepository
          build-root-directory: ./viewmodel
      - id: step-5
        name: publishing viewmodel-react
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-react:publishToSonatype -x :viewmodel-react:compileCommonMainKotlinMetadata closeAndReleaseStagingRepository
          build-root-directory: ./viewmodel
      - id: step-6
        name: publishing viewmodel-test
        uses: gradle/gradle-build-action@v2
        with:
          arguments: :viewmodel-test:publishToSonatype -x :viewmodel-test:compileCommonMainKotlinMetadata closeAndReleaseStagingRepository
          build-root-directory: ./viewmodel

